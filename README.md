# 🌸 Iris Classification (Decision Tree & Logistic Regression)

A simple machine learning project to classify Iris flower species using:
- Decision Tree Classifier
- Logistic Regression

## 📘 Dataset
The dataset is loaded directly from `sklearn.datasets.load_iris()`.

## 🚀 Run Instructions

1. Clone this folder or create a directory:
   ```bash
   1.git clone <repo-url>
   2.cd iris-classification
   3.pip install -r requirements.txt
